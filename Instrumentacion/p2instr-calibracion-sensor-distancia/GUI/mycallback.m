function mycallback(obj, eventdata, polcal)
    %Obtenemos medicion
    %[voltage overVoltage errorcode idnum] = eanalogin(-1, 0, 8, 0);
    %Calculo de distancia
    %salida = polyval(polcal, voltage) 
    %Opcional: Calculo del error
    %(con +- error_max y polyval)
    disp('HOLA PERRETOSO')
end